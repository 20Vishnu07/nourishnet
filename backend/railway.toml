[build]
builder = "NIXPACKS"
buildCommand = "alembic upgrade head && python -m app.ml.train"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[healthcheck]
path = "/health"
timeout = 30
